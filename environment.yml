name: portfolio-projects
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - sqlite
  - pip
  - pip:
      - flask         # Project 2 web API
      - openpyxl      # Project 2 Excel export/import
      - tabulate      # Project 2 table formatting
      - pandas        # Project 2 data analysis